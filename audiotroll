#!/usr/bin/env bash

if [ $1 ]
then
	ffmpeg -i "$1" -f s32le -ar 44100 -ac 1 -y tmp1.wav
	ffmpeg -f f32le -i tmp1.wav -y tmp2.wav
	ffmpeg -i tmp2.wav -f mp3 -y trolled.mp3
	rm tmp1.wav tmp2.wav
else
	echo "Usage: audiotroll <input file>"
fi
